# noteList

`listNotes` — an exhaustive, sorted enumeration of every indexed note,
optionally restricted to a folder prefix. Unlike `store/search/`'s BM25
queries this is not recall-limited: it's the basis for auditing a whole
feature folder.
