# search (command)

The `memory search` subcommand: resolves the target workspace, runs
`store/`'s fused retrieval, and prints each hit via `search.formatter.ts` —
or `(no hits)` when there are none.
