# reindex (command)

The `memory reindex` subcommand: resolves the target workspace(s), rebuilds
each one's index via `store/`'s `buildIndex`, and prints one +/~/- summary
line per workspace in registry order.
