# seed

Seeds `~/.claude/memory/registry.toml` from `<repo>/registry.example.toml`
the first time install runs. Never overwrites a registry that already
exists — a real registry is the user's data, not an installed artifact.
