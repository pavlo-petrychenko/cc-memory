# worklogFormat

Renders the two worklog file formats: `STATE.md` (the living per-worktree
state file) and one append-only journal entry.

These strings are a contract with every worklog already written into a vault
— stay exact, character for character, if you touch either template.
