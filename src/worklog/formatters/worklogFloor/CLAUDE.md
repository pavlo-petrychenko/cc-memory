# worklogFloor

Renders the `SessionEnd` deterministic worklog-floor skeleton: the exact
HTML-comment block a killed session still leaves behind in today's journal
(branch, uncommitted diff summary, recent commits, or "no git activity").

This text is agent-visible — keep it exact if you touch it.
