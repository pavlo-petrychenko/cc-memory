# workingMemory

Renders the working-memory block injected at `SessionStart`: the current
worktree's `STATE.md` content, or the "no working memory yet" variant when
none exists.

This text is agent-visible — keep both phrasings exact if you touch it.
