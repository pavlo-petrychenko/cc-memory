# cli/help

`-h`/`--help` (and a bare `memory` with no arguments) and `--version`. The
usage text is not parsed by any skill, so its wording is free to change; what
is required is that both exit 0 and `--help` lists the real command surface.
